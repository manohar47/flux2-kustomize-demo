apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: infrastructure
  namespace: nginx
spec:
  interval: 30s
  sourceRef:
    kind: GitRepository
    name: test-source
  path: ./infrastructure
  prune: true
